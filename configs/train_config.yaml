seed: 42

data:
  train_dir: data/processed/train
  val_dir: data/processed/val

model:
  name: unet
  in_channels: 3
  out_channels: 1

training:
  epochs: 50
  batch_size: 8
  learning_rate: 0.001

outputs:
  dir: outputs
